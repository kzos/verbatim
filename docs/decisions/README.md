# `docs/decisions/` — decision records

`NNNN-<slug>.md`, one per decision, each carrying the alternative that was rejected and why. The
project's first technology choices become DR-0001 onward.

A decision record without a rejected alternative is a description, not a decision.
